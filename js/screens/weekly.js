export function renderWeekly(container) {
  container.innerHTML = `
    <h1 class="screen-title">Weekly</h1>
    <section class="card">
      <p class="hint">Weekly completion, duration, and calorie stats arrive in a later milestone.</p>
    </section>
  `;
}
