export function renderFood(container) {
  container.innerHTML = `
    <h1 class="screen-title">Food</h1>
    <section class="card">
      <p class="hint">Photo-based meal analysis arrives in a later milestone.</p>
    </section>
  `;
}
