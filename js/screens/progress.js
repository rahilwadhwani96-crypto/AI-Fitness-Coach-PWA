export function renderProgress(container) {
  container.innerHTML = `
    <h1 class="screen-title">Progress</h1>
    <section class="card">
      <p class="hint">Your weight graph and goal tracking arrive in a later milestone.</p>
    </section>
  `;
}
